# API Key 存在这里，绝不能提交
config.json

# 聊天记录，里面有用户的全部对话，跟 API Key 一样不能提交
chat.json
# 原子写的中间文件。写到一半进程死了它就会留在项目根，
# 里面同样是整份聊天记录，不忽略的话 git add . 就跟着提交了
chat.json.tmp

# config.save() 也改成原子写了，于是有同一个中间文件 —— 它装的是整份配置，
# **含 API Key**，不忽略的话 git add . 一样会卷进版本库
config.json.tmp

# 源码方式运行时 paths.DATA_DIR 就是仓库根，于是「编辑人设」会在根目录生成一份
# persona.md —— 那里面是用户自己的设定，跟 config.json 一样不该进版本库。
# 前导斜杠是有意的：只匹配仓库根这一份，随包的 assets/persona.md 不在这条规则里
# （不带斜杠的写法会连它一起匹配上，虽然跟踪中的文件不受 .gitignore 影响，
# 但 `git check-ignore assets/persona.md` 会报「被忽略」，看着像出了事）。
/persona.md

# 她的长期记忆（「记住」工具写的）。源码方式运行时同样落在仓库根，
# 里面是用户跟她说过的私事 —— 和 chat.json 一个性质，绝不能提交。
# 前导斜杠的理由见上一条。
/memory.md
/memory.md.tmp

# 崩溃时写的堆栈，机器相关，不进版本库
taffy.log

# 语音库：克隆真人声音的产物（预渲染 wav + index.json）。
#
# **不进版本库，但会打进安装包。** 这是权衡之后定的：要的是「GitHub 上发出去的
# 安装包装完和本机一致」，README 顶上有对应的免责声明。于是仓库（源码）里只有
# 读取它的 voice.py，音频本身既不进 git，又能被 installer\taffy-pet.iss 收进去。
# 前导斜杠是有意的：只匹配仓库根这份（源码方式运行时 DATA_DIR 就是仓库根），
# 别把将来可能放进 assets/ 的随包音效一起匹配掉。
/voice/

# 跳舞精灵表：从视频素材里逐帧抠出来的第三方画面。同上 —— 不进版本库，但进安装包。
# 前导斜杠的理由和上面一样（源码方式运行时 DATA_DIR 就是仓库根）。
/dance/

# 素材总目录：原片、分离音频、试听带、各种中间产物，好几个 G。
# 它现在**和仓库放在同一个文件夹下**（整个项目收在 D:\MyClaudeProjects\taffy-pet），
# 所以必须挡住 —— 漏了这一条，一句 `git add .` 就把全部素材卷进版本库了。
/_work/

__pycache__/
*.py[cod]
.venv/
venv/

# 仅供目检的中间图，能由 tools/build_assets.py 重新生成
assets/_*.png

# PyInstaller 和 Inno Setup 的产物，加起来一百多兆，要发布走 GitHub Release 的附件，
# 不进版本库
build/
dist/
installer/Output/
